<h1>投稿一覧</h1>
<% @lists.each do |list| %>

    <p>タイトル</p>
    <span>
        <!-- <span><%#= list.title %></span> を削除して、以下コードに変更 -->
        <%#= link_to list.title, "/lists/#{list.id}" %>
        <%= link_to list.title,list_path(list.id) %> <!--見やすい-->
        
    </span>

<% end %>